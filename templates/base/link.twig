{% if href is not empty %}
  {% set url = href %}
{% endif %}

{% if cta is not empty %}
  {% set title = cta %}
{% endif %}

{% if link is not empty %}
  {% set url = link.url %}
  {% set title = link.title %}
  {% set ariaLabel = link.ariaLabel %}
  {% set svg = link.svg %}
  {% set svgAfter = link.svgAfter %}
  {% set target = link.target %}
  {% set class = link.class %}
{% endif %}
<a class="link {{ class }}" href={{ url }} {% if target is not empty %}target="{{target}}"{% endif %} {% if ariaLabel is not empty %}aria-label="{{ ariaLabel }}"{% endif %}>{% if svg is not empty %}{{ svg|raw }}{% endif %}{{ title }}{% if svgAfter is not empty %}{{ svgAfter|raw }}{% endif %}</a>
